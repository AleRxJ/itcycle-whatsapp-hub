package com.itcycle.whatsapp.domain.model;

/**
 * ConversationStatus - status of a conversation.
 * Value object representing conversation lifecycle states.
 */
public enum ConversationStatus {
    ACTIVE,
    CLOSED,
    ARCHIVED
}
